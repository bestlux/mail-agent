{
  "name": "@iomancer/mail-agent-shared",
  "version": "0.1.0",
  "description": "Shared runtime, config, policy, and secret-handling utilities for Mail Agent.",
  "type": "module",
  "license": "MIT",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "node -e \"require('node:fs').rmSync('dist', { recursive: true, force: true })\"",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "publishConfig": {
    "access": "public"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/bestlux/mail-agent.git"
  },
  "homepage": "https://github.com/bestlux/mail-agent",
  "bugs": {
    "url": "https://github.com/bestlux/mail-agent/issues"
  },
  "dependencies": {
    "fast-xml-parser": "5.5.12",
    "keytar": "7.9.0",
    "zod": "4.3.6"
  },
  "devDependencies": {
    "@types/keytar": "4.4.2",
    "@types/node": "25.5.0",
    "typescript": "6.0.2",
    "vitest": "4.1.2"
  },
  "keywords": [
    "codex",
    "mail",
    "fastmail",
    "runtime",
    "secrets"
  ]
}
