# Forward Workflow

- Use `read_thread` or `read_message_batch` before forwarding so the context note is accurate.
- When forwarding, summarize why the message matters before pasting detail-heavy content.
- Preserve links, dates, and named owners from the source message.
